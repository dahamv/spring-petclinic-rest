image:
  file: .gitpod.Dockerfile

tasks:
  - command: npm install -g heroku
  - command: export DATABASE_URL=jdbc:postgresql://localhost:5432/petclinic
  - command: export DATABASE_USERNAME=postgres
  - command: export DATABASE_PASSWORD=
